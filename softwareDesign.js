// Software design

/* 
1. MVC --> folder view
    - interface (all interfaces)
    - routes (all routes)
    - models (all models)
    - views (template engines like ejs, pugs. library/frameworks like react, vue.)
    - controllers (all controllers)


2. Modular Pattern --> section view*
    - student module (interface, routes, models, views, controllers)
    - admin module (interface, routes, models, views, controllers)
    - so on..
*/

// Rule/Principle

/* 
- Dry: Do not repeat yourself
- Fat model/Thin model
*/

// in js

/* 
1. Schema
2. Model
3. DB Query 
*/

// in ts

/* 
1. Interface
2. Schema
3. Model
4. DB Query
*/
